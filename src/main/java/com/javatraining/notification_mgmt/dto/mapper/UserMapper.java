/*
package com.javatraining.notification_mgmt.dto.mapper;


import com.javatraining.notification_mgmt.dto.request.UserRequestDto;
import com.javatraining.notification_mgmt.dto.response.UserResponseDto;
import com.javatraining.notification_mgmt.model.User;
import org.mapstruct.Mapper;

//tells MapStruct to generate a Spring bean, so you can inject it using @Autowired or constructor injection.
@Mapper(componentModel = "spring")
public interface UserMapper {

    // Maps request → entity
    User toEntity(UserRequestDto request);

    // Maps entity → response
    UserResponseDto toDto(User user);
}
*/
